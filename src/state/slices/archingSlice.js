import { createSlice } from "@reduxjs/toolkit";

const initialState = {

}

const archingSlice = createSlice({
    name: 'arching',
    initialState,
    reducers: {

    },
});

export const {

} = archingSlice.actions;

export default archingSlice.reducer;